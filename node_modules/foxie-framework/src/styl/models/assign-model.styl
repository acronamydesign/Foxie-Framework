{column-1}
	col(1)
{column-2}
	col(2)
{column-3}
	col(3)
{column-4}
	col(4)
{column-5}
	col(5)
{column-6}
	col(6)
{column-7}
	col(7)
{column-8}
	col(8)
{column-9}
	col(9)
{column-10}
	col(10)
{column-11}
	col(11)
{column-12}
	col(12)


//navbar
{navbar-compact}
	navbar(compact)
{navbar-full}
	navbar(full)
{navbar-compact-ctrl-right}
	navbar(compact, right)
{navbar-full-ctrl-right}
	navbar(full, right)
{navbar-compact-ctrl-left}
	navbar(compact, left)
{navbar-full-ctrl-left}
	navbar(full, left)

//logo
{logo-default}
	logo()
{logo-light}
	logo(light)
{logo-dark}
	logo(dark)
{logo-negative}
	logo(inversion)

//container
{container-default}
	container()
{container-fluid}
	container(fluid)

//list-group
{list-default}
	list-group()
{list-selectable}
	list-group(selectable)
{list-selectable-hairline}
	list-group(selectable hairline)

//Buttons
{btn-default}
	btn()
{btn-ghost}
	btn(default, ghost)
{btn-invisible}
	btn(default, invisible)

{inputs-default}
	input()

{hero-default}
	hero(null, #000)

{notify-stack}
	notification-stack()
{notify-warning}
	notification(warning)
{notify-info}
	notification(info)
{notify-success}
	notification(success)
{notify-attention}
	notification(attention)

{dropmenu-default}
	dropdown(left)
{dropmenu-right}
	dropdown(right)
{dropmenu-active}
	dropdown(active)