html
	box-sizing:box;
	-webkit-font-smoothing: antialiased;
	font-size:base-font-size;

	body
		font-size: 14px;

::selection
	if primary-color
		background: lighten(primary-color, 68%);
::-moz-selection
	if primary-color
		background: lighten(primary-color, 68%);