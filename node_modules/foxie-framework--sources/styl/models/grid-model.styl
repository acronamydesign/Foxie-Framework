//Grid - tune the grid

allCol = grid.ammount;
oneCol = 100 / grid.ammount;
oneCol = oneCol + measure;
gutter = grid.gutter;
container-width = grid.container;
small-container-width = grid.small-container;