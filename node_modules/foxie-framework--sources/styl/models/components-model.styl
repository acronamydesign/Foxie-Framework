//common sizes
common = {
	input-height: 2rem,
}

input-states()
	transition:box-shadow .2s;
	&:focus
		border:primary-color 1px solid;
		-webkit-box-shadow: 0px 0px 5px darken(primary-color, 60%);

input-height = common.input-height;
