var ff = require('../core/ff-assign.json');
var _ = require('underscore')

for(var key in ff) {
	var value = ff[key];

	console.log(value)
	console.log(key)

	//var components = Object.push(key)

	//console.log(components)
	for(var prop in value){
		var selector = $(value[prop]);
	}
}